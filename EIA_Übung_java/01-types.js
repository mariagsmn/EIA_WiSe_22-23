// 1. Datentyp: number
let x = 42; // lokale Variable (nur für bestimmte Sachen anwendbar durch 'let')
const pi = 3.1415; //globale Variable (innerhalb ganzem Code anwendbar) //const = nicht veränderbat, let = veränderbare Variable

let result = 23 / 0;

// 2. Datentyp: string (alles was wie text/buchstaben aussieht ist string)
let text = 'Hallo Welt'
text = "Hallo Welt" //egal ob 1 oder 2 Striche, aber immer das gleiche in einem Dokument

// 3. Datentyp: boolean (nur 2 mögliche Werte)
let isSunny = true;
let isRainy = false;

// 4. Datentyp: undefined (nur ein möglicher Wert)
x = undefined;

// 5. Datentyp: null (nur ein möglicher Wert)
x = null;

// Wertetypen: number 
// Referenztypen: string (kann nicht verändert werden)

0 === '' //gleich
0 !== '' //ungleich

